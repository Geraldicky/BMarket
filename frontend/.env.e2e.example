# Optional: run tests against an already deployed frontend instead of local Expo web.
# PLAYWRIGHT_BASE_URL=https://b-market-hazel.vercel.app

# Optional authenticated smoke. Use demo seed credentials or dedicated test accounts.
E2E_STUDENT_EMAIL=demo.buyer@binus.ac.id
E2E_STUDENT_PASSWORD=demo12345
E2E_ADMIN_EMAIL=demo.admin@binus.ac.id
E2E_ADMIN_PASSWORD=demo12345

# Optional API health smoke.
E2E_API_URL=http://localhost:3000/api
