PORT=3000
NODE_ENV=development

# Local PostgreSQL:
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/bmarket?schema=public"
# Production Supabase: gunakan Connect -> Supavisor Session Pooler (port 5432).
# Batasi pool per Railway replica agar interactive transaction tidak menghabiskan
# kapasitas Supavisor; sesuaikan 5 dengan batas koneksi project dan jumlah replica.
# DATABASE_URL="postgresql://postgres.PROJECT_REF:PASSWORD@aws-0-REGION.pooler.supabase.com:5432/postgres?schema=public&connection_limit=5&pool_timeout=10"

# Buat dengan: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="ganti_dengan_secret_acak_minimal_32_karakter"
JWT_EXPIRES_IN="7d"

SSO_ALLOWED_DOMAINS="@binus.ac.id,@student.binus.ac.id,@binus.edu"

OTP_HASH_SECRET="ganti_dengan_secret_otp_acak_minimal_32_karakter"
OTP_TTL_MINUTES=10
OTP_RESEND_SECONDS=60
OTP_MAX_ATTEMPTS=5
PASSWORD_RESET_MIN_RESPONSE_MS=350

# Production gratis (Render Free): gunakan Brevo Transactional Email API melalui HTTPS.
BREVO_API_KEY=""
BREVO_FROM_EMAIL=""
BREVO_FROM_NAME="BMarket"

# SMTP tetap tersedia sebagai fallback untuk local development/non-Render.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="BMarket <your-email@gmail.com>"
OTP_DEV_LOG=true

# Bisa berisi beberapa origin dipisahkan koma.
CORS_ORIGIN="http://localhost:8081,http://localhost:19006"

# Local fallback. Production Render memakai Supabase Storage di bawah.
UPLOAD_DIR="uploads"
PUBLIC_BASE_URL=""

# Supabase Storage. Bucket listing image sebaiknya PUBLIC.
SUPABASE_URL=""
SUPABASE_SERVICE_ROLE_KEY=""
SUPABASE_STORAGE_BUCKET="bmarket-public"

CHECKOUT_RESERVATION_MINUTES=15

# Midtrans Snap Sandbox (jangan gunakan/commit key production).
MIDTRANS_SERVER_KEY=""
MIDTRANS_CLIENT_KEY=""
MIDTRANS_IS_PRODUCTION=false
# Opsional. Placeholder dinamis yang didukung: {transactionId}, {orderId}
# MIDTRANS_FINISH_URL="https://binus-market.vercel.app/transaction/{transactionId}"
